export default function Contact(props: any) {
	return <div>
		<p>Please feel free to <a href={props.contactEmail}>message me</a> if you want to talk about development or if you want a cat pic!</p>
	</div>
}